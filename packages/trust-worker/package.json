{
  "name": "@trustvault/trust-worker",
  "version": "0.1.0",
  "private": true,
  "description": "TrustVault agent — Cloudflare Worker + Durable Object for verifiable trust resumes via ERC-8004",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.5.0",
    "wrangler": "^3.65.0"
  }
}
